node_modules/
.env
users.json
*.log
.cursor/

# User uploads (recreate via app)
Uploads/

# Default uploads used as fallback - keep out of repo to avoid Render size limits (use external hosting or minimal set only)
default-uploads/

# OS files
.DS_Store
Thumbs.db
